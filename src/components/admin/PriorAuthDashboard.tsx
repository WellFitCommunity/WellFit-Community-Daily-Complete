/**
 * PriorAuthDashboard — Thin re-export from decomposed subdirectory
 *
 * Preserves existing lazy import path:
 *   lazy(() => import('../PriorAuthDashboard'))
 */

export { default } from './prior-auth';

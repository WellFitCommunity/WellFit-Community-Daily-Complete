# npm configuration for WellFit Community platform
# This ensures consistent dependency resolution across all environments

# Use legacy peer deps during ESLint 9 ecosystem transition
# This is the recommended approach while waiting for all plugins to update
# See: https://docs.npmjs.com/cli/v10/using-npm/config#legacy-peer-deps
legacy-peer-deps=true

# Automatically save exact versions (no ^ or ~)
# save-exact=true

# Use npm lockfile version 2 (compatible with npm 7+)
# lockfile-version=2

# `ml/models/` — Training + Evaluation (Phase 2 — GATED)

Offline-only model training/evaluation. **Gated on real labeled outcome data**
(the Phase 1 readiness report) **and Maria's sign-off to start.** Do not invent an
algorithm or fabricate data to fill this. Artifacts carry an explicit pinned
`model_version` (`.claude/rules/python.md` §5); validate frame schema on ingest (§9).
Models are **advisory only** (§10). Empty placeholder until Phase 2.

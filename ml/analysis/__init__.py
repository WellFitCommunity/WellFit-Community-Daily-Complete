"""Read-only analysis. Phase 1 — see ml/analysis/README.md.

Import directly from submodules (e.g. `from ml.analysis.readiness import
assess_readiness`) — no eager re-exports here, so `python -m
ml.analysis.readiness` runs without a double-import warning.
"""

# .github/workflows/ci-cd.yml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop, 'claude/**' ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch: # Allow manual trigger

env:
  NODE_VERSION: '20'
  NOTIFICATION_EMAILS: "info@thewellfitcommunity.org"

jobs:
  # ============================================
  # Job 1: Type Checking
  # ============================================
  typecheck:
    name: TypeScript Type Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # ENFORCED: Quality gate - typecheck must pass for pilot/grant readiness

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Run TypeScript type check
        id: typecheck
        run: npm run typecheck

      - name: Report to summary
        if: always()
        run: |
          echo "## 🔍 TypeScript Type Check" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.typecheck.outcome }}" == "success" ]; then
            echo "✅ TypeScript type check passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Type check failed - must fix before merge" >> $GITHUB_STEP_SUMMARY
          fi

  # ============================================
  # Job 2: Linting
  # ============================================
  lint:
    name: ESLint Code Quality
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # ENFORCED: Quality gate - lint must pass for pilot/grant readiness

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Run ESLint
        id: eslint
        run: npm run lint

      - name: Report to summary
        if: always()
        run: |
          echo "## 🔧 ESLint Code Quality" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.eslint.outcome }}" == "success" ]; then
            echo "✅ ESLint passed with no errors" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ ESLint failed - must fix before merge" >> $GITHUB_STEP_SUMMARY
          fi

  # ============================================
  # Job 3: Unit Tests (Parallel - 4 shards)
  # ============================================
  test-unit:
    name: Unit Tests (${{ matrix.shard }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        shard: [components, services, pages, other]
        include:
          - shard: components
            pattern: "src/components/"
            name: "Components"
          - shard: services
            pattern: "src/services/"
            name: "Services"
          - shard: pages
            pattern: "src/pages/"
            name: "Pages"
          - shard: other
            pattern: "src/(adapters|types|utils|contexts|hooks|__tests__)/"
            name: "Other"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Run ${{ matrix.name }} tests
        id: unit-tests
        run: npm run test:unit:${{ matrix.shard }}
        env:
          CI: true
          NODE_OPTIONS: "--max-old-space-size=4096"
        continue-on-error: false

      - name: Report to summary
        if: always()
        run: |
          echo "## 🧪 Unit Tests - ${{ matrix.name }}" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.unit-tests.outcome }}" == "success" ]; then
            echo "✅ ${{ matrix.name }} tests passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ ${{ matrix.name }} tests failed" >> $GITHUB_STEP_SUMMARY
          fi

  # ============================================
  # Job 4: Build Application
  # ============================================
  build:
    name: Build Application
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [typecheck, lint]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Create .env file for build
        run: |
          touch .env
          # Use secrets if available, otherwise use placeholders
          # Note: Vite uses VITE_ prefix, not REACT_APP_
          if [ -n "${{ secrets.VITE_SUPABASE_URL }}" ]; then
            echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" >> .env
          else
            echo "VITE_SUPABASE_URL=https://placeholder.supabase.co" >> .env
          fi

          if [ -n "${{ secrets.VITE_SUPABASE_ANON_KEY }}" ]; then
            echo "VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env
          else
            echo "VITE_SUPABASE_ANON_KEY=placeholder-key" >> .env
          fi

      - name: Build application
        run: npm run build
        env:
          CI: true
          GENERATE_SOURCEMAP: false

      - name: Check build output
        run: |
          if [ ! -d "build" ]; then
            echo "❌ Build directory not found"
            exit 1
          fi

          BUILD_SIZE=$(du -sh build | cut -f1)
          echo "## 📦 Build Complete" >> $GITHUB_STEP_SUMMARY
          echo "✅ Build successful" >> $GITHUB_STEP_SUMMARY
          echo "- Build size: $BUILD_SIZE" >> $GITHUB_STEP_SUMMARY
          echo "- Build directory: build/" >> $GITHUB_STEP_SUMMARY

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: build/
          retention-days: 7

  # ============================================
  # Job 5: Integration Tests (Optional)
  # ============================================
  test-integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: build
    # Only run if build succeeded
    if: success()

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: npm install --legacy-peer-deps --ignore-scripts

      - name: Run integration tests
        id: integration-tests
        run: npm run test:integration
        env:
          CI: true
        continue-on-error: true

      - name: Report to summary
        if: always()
        run: |
          echo "## 🔗 Integration Tests" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.integration-tests.outcome }}" == "success" ]; then
            echo "✅ Integration tests passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Integration tests had issues (non-blocking)" >> $GITHUB_STEP_SUMMARY
          fi

  # ============================================
  # Job 5b: MCP Edge Function Integration Tests
  # ============================================
  test-mcp-integration:
    name: MCP Edge Function Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: build
    # ENFORCED: Catches runtime errors like "logger.log is not a function"
    if: success()

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Run MCP Integration Tests
        id: mcp-tests
        run: |
          cd supabase/functions/__tests__
          deno test --allow-net --allow-env mcp-integration.test.ts 2>&1 | tee mcp-test-output.txt
          exit ${PIPESTATUS[0]}
        env:
          SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        continue-on-error: false

      - name: Report to summary
        if: always()
        run: |
          echo "## 🔌 MCP Edge Function Tests" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.mcp-tests.outcome }}" == "success" ]; then
            echo "✅ All MCP servers responding correctly" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Verified:" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-prior-auth-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-claude-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-npi-registry-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-cms-coverage-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-clearinghouse-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-fhir-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-postgres-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-hl7-x12-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-medical-codes-server" >> $GITHUB_STEP_SUMMARY
            echo "- mcp-edge-functions-server" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ MCP server tests failed - RUNTIME ERRORS DETECTED" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "This catches errors like 'logger.log is not a function' that unit tests miss." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Test Output</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat supabase/functions/__tests__/mcp-test-output.txt >> $GITHUB_STEP_SUMMARY 2>/dev/null || echo "No output captured"
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

  # ============================================
  # Job 6: Bundle Analysis
  # ============================================
  analyze-bundle:
    name: Bundle Size Analysis
    runs-on: ubuntu-latest
    needs: build
    # Only run if build succeeded
    if: success()

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build-output
          path: build/

      - name: Analyze bundle size
        run: |
          echo "## 📊 Bundle Size Analysis" >> $GITHUB_STEP_SUMMARY

          # Find main JS bundle
          MAIN_JS=$(find build/static/js -name "main.*.js" | head -1)

          if [ -f "$MAIN_JS" ]; then
            MAIN_SIZE=$(du -h "$MAIN_JS" | cut -f1)
            echo "- Main bundle: $MAIN_SIZE" >> $GITHUB_STEP_SUMMARY
          else
            echo "- Main bundle: Not found" >> $GITHUB_STEP_SUMMARY
          fi

          # Total JS size
          JS_SIZE=$(du -sh build/static/js 2>/dev/null | cut -f1 || echo "N/A")
          CSS_SIZE=$(du -sh build/static/css 2>/dev/null | cut -f1 || echo "N/A")
          TOTAL_SIZE=$(du -sh build 2>/dev/null | cut -f1 || echo "N/A")

          echo "- JavaScript: $JS_SIZE" >> $GITHUB_STEP_SUMMARY
          echo "- CSS: $CSS_SIZE" >> $GITHUB_STEP_SUMMARY
          echo "- Total: $TOTAL_SIZE" >> $GITHUB_STEP_SUMMARY
          echo "✅ Bundle analysis complete" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # Job 7: Final Summary
  # ============================================
  ci-summary:
    name: CI/CD Summary
    runs-on: ubuntu-latest
    needs: [typecheck, lint, test-unit, build, test-mcp-integration]
    if: always()

    steps:
      - name: Check job statuses
        run: |
          echo "## 🎯 CI/CD Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY

          # Check each job status
          TYPECHECK="${{ needs.typecheck.result }}"
          LINT="${{ needs.lint.result }}"
          TEST="${{ needs.test-unit.result }}"
          BUILD="${{ needs.build.result }}"
          MCP="${{ needs.test-mcp-integration.result }}"

          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY

          if [ "$TYPECHECK" == "success" ]; then
            echo "| TypeScript | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| TypeScript | ⚠️ Non-blocking |" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "$LINT" == "success" ]; then
            echo "| ESLint | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ESLint | ⚠️ Non-blocking |" >> $GITHUB_STEP_SUMMARY
          fi

          # Test matrix result - success only if ALL shards passed
          if [ "$TEST" == "success" ]; then
            echo "| Unit Tests (4 shards) | ✅ All Passed |" >> $GITHUB_STEP_SUMMARY
          elif [ "$TEST" == "cancelled" ]; then
            echo "| Unit Tests | ⏸️ Cancelled |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| Unit Tests | ❌ Some Failed |" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "$BUILD" == "success" ]; then
            echo "| Build | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| Build | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi

          # MCP Edge Function Tests - CRITICAL for catching runtime errors
          if [ "$MCP" == "success" ]; then
            echo "| MCP Edge Functions | ✅ All Servers Healthy |" >> $GITHUB_STEP_SUMMARY
          elif [ "$MCP" == "skipped" ]; then
            echo "| MCP Edge Functions | ⏭️ Skipped |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| MCP Edge Functions | ❌ Runtime Errors Detected |" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY

          # Fail if critical jobs (build, tests, MCP) failed or were cancelled
          if [ "$TEST" != "success" ] || [ "$BUILD" != "success" ] || [ "$MCP" == "failure" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "❌ **Pipeline Failed** - Critical jobs failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # Provide specific guidance based on failure type
            if [ "$TEST" == "cancelled" ]; then
              echo "**Test Job Timeout Detected:**" >> $GITHUB_STEP_SUMMARY
              echo "- The test job exceeded the 30-minute timeout" >> $GITHUB_STEP_SUMMARY
              echo "- Check for hanging tests or infinite loops" >> $GITHUB_STEP_SUMMARY
              echo "- Consider optimizing slow tests or splitting into parallel jobs" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            if [ "$MCP" == "failure" ]; then
              echo "**MCP Edge Function Tests Failed:**" >> $GITHUB_STEP_SUMMARY
              echo "- Runtime errors detected in deployed edge functions" >> $GITHUB_STEP_SUMMARY
              echo "- Common cause: logger.log() instead of logger.info()" >> $GITHUB_STEP_SUMMARY
              echo "- Check the MCP Edge Function Tests job for details" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            echo "Note: TypeScript and ESLint are non-blocking and can be fixed incrementally" >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ **All Critical Checks Passed** - Ready for deployment" >> $GITHUB_STEP_SUMMARY
            if [ "$TYPECHECK" != "success" ] || [ "$LINT" != "success" ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "⚠️ Note: TypeScript/ESLint have warnings - consider fixing incrementally" >> $GITHUB_STEP_SUMMARY
            fi
          fi

      - name: Send email notification
        if: always() && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        run: |
          # Determine status
          if [ "${{ job.status }}" == "success" ]; then
            STATUS_EMOJI="✅"
            STATUS_TEXT="SUCCESS"
            STATUS_COLOR="#28a745"
          else
            STATUS_EMOJI="❌"
            STATUS_TEXT="FAILURE"
            STATUS_COLOR="#dc3545"
          fi

          # Send email via MailerSend API
          curl -X POST "https://api.mailersend.com/v1/email" \
            -H "Authorization: Bearer ${{ secrets.MAILERSEND_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{
              "from": {
                "email": "ci-cd@thewellfitcommunity.org",
                "name": "WellFit CI/CD Pipeline"
              },
              "to": [
                {
                  "email": "'"${{ env.NOTIFICATION_EMAILS }}"'",
                  "name": "Development Team"
                }
              ],
              "subject": "CI/CD Pipeline - '"${{ github.ref_name }}"' - '"$STATUS_TEXT"'",
              "text": "CI/CD Pipeline Status: '"$STATUS_TEXT"'\n\nBranch: ${{ github.ref_name }}\nCommit: ${{ github.sha }}\nActor: ${{ github.actor }}\n\nView details: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}",
              "html": "<!DOCTYPE html><html><body style=\"font-family: Arial, sans-serif;\"><div style=\"background-color: '"$STATUS_COLOR"'; color: white; padding: 20px; text-align: center;\"><h1>'"$STATUS_EMOJI"' CI/CD Pipeline '"$STATUS_TEXT"'</h1></div><div style=\"padding: 20px;\"><p><strong>Branch:</strong> ${{ github.ref_name }}</p><p><strong>Commit:</strong> <code>${{ github.sha }}</code></p><p><strong>Actor:</strong> ${{ github.actor }}</p><p style=\"text-align: center; margin-top: 30px;\"><a href=\"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" style=\"background-color: #0066cc; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; display: inline-block;\">View Full Report</a></p></div></body></html>"
            }' || echo "⚠️ Email notification failed"
